#import <Foundation/Foundation.h>

#if __has_attribute(swift_private)
#define AC_SWIFT_PRIVATE __attribute__((swift_private))
#else
#define AC_SWIFT_PRIVATE
#endif

/// The "Celluar" asset catalog image resource.
static NSString * const ACImageNameCelluar AC_SWIFT_PRIVATE = @"Celluar";

/// The "Connecting0" asset catalog image resource.
static NSString * const ACImageNameConnecting0 AC_SWIFT_PRIVATE = @"Connecting0";

#warning The "Connecting0_" image asset name resolves to the symbol "ACImageNameConnecting0" which already exists. Try renaming the asset.

/// The "Connecting1" asset catalog image resource.
static NSString * const ACImageNameConnecting1 AC_SWIFT_PRIVATE = @"Connecting1";

#warning The "Connecting1_" image asset name resolves to the symbol "ACImageNameConnecting1" which already exists. Try renaming the asset.

/// The "Connecting2" asset catalog image resource.
static NSString * const ACImageNameConnecting2 AC_SWIFT_PRIVATE = @"Connecting2";

#warning The "Connecting2_" image asset name resolves to the symbol "ACImageNameConnecting2" which already exists. Try renaming the asset.

/// The "Connecting3" asset catalog image resource.
static NSString * const ACImageNameConnecting3 AC_SWIFT_PRIVATE = @"Connecting3";

#warning The "Connecting3_" image asset name resolves to the symbol "ACImageNameConnecting3" which already exists. Try renaming the asset.

/// The "Offline" asset catalog image resource.
static NSString * const ACImageNameOffline AC_SWIFT_PRIVATE = @"Offline";

#warning The "Offline_" image asset name resolves to the symbol "ACImageNameOffline" which already exists. Try renaming the asset.

/// The "Signal0" asset catalog image resource.
static NSString * const ACImageNameSignal0 AC_SWIFT_PRIVATE = @"Signal0";

#warning The "Signal0_" image asset name resolves to the symbol "ACImageNameSignal0" which already exists. Try renaming the asset.

/// The "Signal1" asset catalog image resource.
static NSString * const ACImageNameSignal1 AC_SWIFT_PRIVATE = @"Signal1";

#warning The "Signal1_" image asset name resolves to the symbol "ACImageNameSignal1" which already exists. Try renaming the asset.

/// The "Signal2" asset catalog image resource.
static NSString * const ACImageNameSignal2 AC_SWIFT_PRIVATE = @"Signal2";

#warning The "Signal2_" image asset name resolves to the symbol "ACImageNameSignal2" which already exists. Try renaming the asset.

/// The "Signal3" asset catalog image resource.
static NSString * const ACImageNameSignal3 AC_SWIFT_PRIVATE = @"Signal3";

#warning The "Signal3_" image asset name resolves to the symbol "ACImageNameSignal3" which already exists. Try renaming the asset.

/// The "Signal4" asset catalog image resource.
static NSString * const ACImageNameSignal4 AC_SWIFT_PRIVATE = @"Signal4";

#warning The "Signal4_" image asset name resolves to the symbol "ACImageNameSignal4" which already exists. Try renaming the asset.

/// The "app" asset catalog image resource.
static NSString * const ACImageNameApp AC_SWIFT_PRIVATE = @"app";

#undef AC_SWIFT_PRIVATE
